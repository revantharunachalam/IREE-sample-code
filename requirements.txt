# Requires Python 3.11, 3.12, or 3.13 on Windows.
# (Python 3.10 has no Windows wheel for iree-base-compiler / iree-base-runtime.)
# On Linux (manylinux x86_64 / aarch64) Python 3.10 also works.

# Core IREE packages
# Renamed in late 2024: iree-compiler -> iree-base-compiler, iree-runtime -> iree-base-runtime.
# The old names still resolve to the final 20241104.1068 release; prefer the new ones.
iree-base-compiler>=3.11.0
iree-base-runtime>=3.11.0

# TensorFlow support
tensorflow>=2.13.0
iree-tools-tf>=20240207.859

# PyTorch support
torch>=2.1.0
torch-mlir>=20240207

# Utilities
numpy>=1.24.0
